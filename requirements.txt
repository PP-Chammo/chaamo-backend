# Core web framework
fastapi==0.116.1
starlette==0.47.2
pydantic==2.11.7
uvicorn[standard]==0.35.0

# HTTP client and web scraping
httpx==0.28.1
beautifulsoup4==4.13.4

# Database and environment
supabase==2.17.0
python-dotenv==1.1.1

# Date/time handling
python-dateutil==2.9.0.post0
pytz==2025.2

# Array processing
numpy==2.1.3

# Embeddings / local fallback
sentence-transformers==5.1.0
torch>=2.1.0           # required by sentence-transformers (CPU/GPU variant). Install appropriate wheel for your platform.

# OpenAI SDK (for text-embedding-3-small, LLM calls)
openai>=1.0.0,<3.0.0

# Optional / production helpers
# faiss-cpu may be useful for local vector search (optional, platform-dependent)
# faiss-cpu==1.7.4

# Testing / dev
pytest>=8.0.0
